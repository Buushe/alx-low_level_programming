#include "main.h"

/**
 * print_rev - print reverse characters.
 * @s: validate the character
 * Return: Always 0.
 */
void _puts(char *str)
{
        for (; i >= 0; i--)
        {
                _putchar(*str);
        }
        _putchar('\n');
}
